{
  "name": "contract-audit-mvp",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "test": "bun test packages apps",
    "typecheck": "tsc --noEmit",
    "dev": "bun --filter @contract-audit/api dev"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@playwright/test": "^1.50.0",
    "typescript": "^7.0.2"
  }
}
